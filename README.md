# myconfig
Using this place to store my configs

Migration from openbox/ratpoison to sway/wayland complete. Deleted all display manager. Run sway after login. 

IBUS -> fcitx5. Pretty straight forward migration. Just need to set up env in .bashrc.

flameshot -> grim + slurp + swappy, but swappy is slow, drop it for now. 
